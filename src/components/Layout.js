import PropTypes from "prop-types";
import React from "react";

// Layout holds all of the content / children at the parent level in the website
const Layout = ({ children }) => {
  return <>{children}</>;
};

export default Layout;
